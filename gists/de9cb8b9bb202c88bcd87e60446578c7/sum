static int sum(List<Integer> list) {
        if (list.isEmpty()) {
            throw new NullPointerException("List is empty");
        }
        
        int sum=0;
        for (int i : list) {
            sum+=i;
        }
        return sum;
    }